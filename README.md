# Backend Imprenta

1. Crear base de datos `imprenta_db` en tu MySQL
2. Copiar `.env.example` a `.env` y completar si hace falta
3. Instalar dependencias con `npm install`
4. Iniciar con `npm run dev`
